{
  "name": "mavo-offline",
  "version": "1.0.0",
  "description": "Backend interceptor for Mavo",
  "author": "Valter Kraemer",
  "license": "MIT",
  "scripts": {
    "build": "babel ./src/mavo-offline.js -o ./dist/mavo-offline.js --presets es2015 --source-maps & babel ./src/mavo-offline.js -o ./dist/mavo-offline.min.js --presets es2015,minify --source-maps"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-minify": "^0.2.0"
  }
}
